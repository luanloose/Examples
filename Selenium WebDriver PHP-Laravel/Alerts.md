# Alerts workthough

* Accept an Alert

        $this->driver->switchTo()->alert()->accept();

* Decline

        $this->driver->switchTo()->alert()->getText();

* Get text

        $this->driver->switchTo()->alert()->getText();
